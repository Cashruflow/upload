# Вход в трекер привычек: посадочная страница (28.08.2026)

Запись о работе. **Код живёт не в этом репозитории**, а на платформе
`ai.cashruflow.ru` (правки внесены через MCP-коннектор). Второй копии файла здесь
намеренно нет: она сразу разошлась бы с боевой.

## Что изменено на платформе

| Файл | Что |
|---|---|
| `web/public/patient-auth.html` | Страница входа `/habits/auth` и `/medcard/auth` — переписана: посадочная и форма входа на одном экране |
| `docs/ADR/ADR-140-habits-auth-landing.md` | Решение и его границы |
| `docs/CHANGELOG.md` | Запись v2.26.0 |
| `docs/VERSION.md` | 2.25.9 → 2.26.0 |
| `docs/modules/habits/VERSION.md` | 1.7.0 → 1.8.0 |

## Суть

`/habits/auth` — единственная дверь в трекер снаружи: на неё ведут реф-ссылки
`/r/{code}` (ADR-134) и туда пойдёт реклама. Была голая форма регистрации без единого
слова о продукте. Стало:

- первый экран — обещание, одно главное действие, макет продукта, «как это работает»,
  «что внутри», приватность, вопросы, финальный призыв;
- макет продукта собран настоящей вёрсткой карточки миссии (не картинкой и не
  скриншотом) — не устаревает при правках трекера;
- бренд `#00a0ff`, палитра трекера, значки из общего спрайта `/assets/icons.svg`,
  эмодзи убраны (запрет из `CLAUDE.md`);
- плашка «Вас пригласил друг» при переходе по реф-ссылке (cookie `ref`);
- экраны регистрации, кода доступа и способов входа переоформлены; логика
  регистрации, passkey и Telegram-привязки не тронута, все id сохранены.

Приёмка в Chromium: 390×844 и 1360×900, горизонтальной прокрутки нет.

## Второй заход (28.08.2026, вечер)

- **Светлая тема.** Палитра переехала в токены, светлая — один блок
  `:root[data-theme="light"]`. Тема ставится до отрисовки скриптом в `<head>`: ручной
  выбор из `localStorage` сильнее системного, нет выбора — `prefers-color-scheme` и
  слежение за его сменой на лету. Переключатель — `i-palette` в шапке. `--brand` в
  светлой темнеет до `#0072c6` (яркий `#00a0ff` на белом — 2.5:1), заливка кнопки
  остаётся фирменной.
- **Плюсы под кнопками**: 5 миссий бесплатно, без подписки, без кредитной карты, без
  рекламы, без телефона и email, работает без сети. Разворот — секция «Честные условия»;
  формулировки сверены с кодом (лимит слотов, отсутствие платёжных полей, sha256 у кода
  доступа), шифрование базы не обещается — его нет.
- **Слот под рекламный кадр на телефоне**: `/assets/habits/hero-mobile.{webp,jpg,png}`.
  Нет файла — блок снимается, остаётся макет из вёрстки; есть — на телефоне кадр вместо
  макета, на десктопе всегда макет. og-тег указывает на `/assets/habits/og.jpg`.
- Приёмка в Chromium в обеих темах на 390×844 и 1360×900; отдельно проверено поведение
  при отсутствующей картинке.

## Третий заход (28.08.2026, ночь)

- **Офлайн.** Причин, почему «без сети сайт не грузится», было две: `/habits/auth` вообще
  не регистрировала service worker (его поднимала только `habits.html`, то есть страница
  за входом), и `habits-sw.js` ничего не клал в кэш на install. Теперь воркер
  регистрируется и со страницы входа, кладёт оболочку поштучно, навигацию ищет с
  `ignoreSearch`, счётчик Метрики не перехватывает. Обещание на странице исправлено:
  «Записи не теряются без сети», офлайн-открытие — после первого захода с интернетом.
- **Согласие на куки**: до выбора Метрика не грузится; экран и регистрация НЕ блокируются
  (технические куки согласия не требуют, заглушка убивала бы конверсию с рекламы).
- **Яндекс.Метрика** только на `/habits/auth`, номер в константе `YM_ID`, webvisor выключен,
  цели `habits_signup` / `habits_login`, переключение экранов шлёт `hit`.
- **h1 на странице один** — заголовки экранов флоу переведены в `h2`.
- Кнопка темы: квадрат 38×38, солнце/луна (`i-sun`/`i-moon` добавлены в общий спрайт точными
  SVG из `lucide-static@1.35.0`) вместо палитры 15px в прямоугольнике.

## Четвёртый заход (28.08.2026, ночь)

- **Счётчики поставлены вручную**: Яндекс.Метрика `50095486` (снипет как есть, `webvisor:true`)
  и Roistat `9188c4290048f491d0dcb9c048decf9d`. Оба грузятся только после согласия.
  Поля псевдонима, даты рождения, кода доступа и плашка с выданным кодом помечены
  `ym-hide-content` — вебвизор не должен их писать.
- **Баннер кук**: кнопки «Принять всё» / «Настроить»; «Настроить» раскрывает тумблер
  аналитики прямо в баннере и превращается в «Сохранить выбор». Блокировки экрана нет.
- **Новая страница `/policy`** — политика обработки данных по фактическому составу данных
  из кода (реквизиты оператора не проставлены, вписать до подачи в Директ). Роут смонтирован
  выше `app.get("/:slug")`, `web-interface` перезапущен, API отвечает.
- **`public/auth-theme.css`** — общие токены палитры для входа и политики, чтобы цвета не
  разъехались копиями.
- Поймано и починено: атрибут `hidden` не работает на элементах с `display:flex`, если
  страница не подключает `/theme.css`. Правило дописано в `docs/rules/ui.md`.

## Осталось

- положить картинки: `/assets/habits/og.jpg` (1200×630, превью ссылки) и
  `/assets/habits/hero-mobile.webp` (вертикальный кадр для телефона) — код их уже ждёт;
- своя посадочная у медкарты (сейчас она получает тот же вид без блоков про миссии);
- кросс-ссылка «медкарта ⇄ привычки» — мешает кука `card_src=habits` (20 минут).
