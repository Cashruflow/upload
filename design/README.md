# /slots — до и после

Рабочие файлы холста «Слоты — до и после».

- `BeforeDesktop.dc.html` — страница как есть, 1440
- `Main.dc.html` — предложение, 1440
- `BeforeMobile.dc.html` — как есть, 390
- `AfterMobile.dc.html` — предложение, 390
- `canvas.json` — раскладка холста и заметки
- `slots-do-i-posle.html` — собранный холст

Исходник страницы: `/home/cashruflow/web/public/slots.html` на сервере
(маршрут `GET /slots` в `web/booking.js`). Значения токенов сняты с
`/ui.css` и `THEME_DEFAULTS` в `web/server.js` (btnCss / inpCss).

## Выкачено в прод

`web/public/slots.html` — файл, который лежит на сервере по пути
`/home/cashruflow/web/public/slots.html` (выкачен 23.08.2026).
Бэкап прежней версии: `/home/cashruflow/backups/slots.html.2026-08-23T20-46-57-488Z.bak`.
